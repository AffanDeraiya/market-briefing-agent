# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Node / frontend
node_modules/
dist/
*.local

# App runtime
.cache/
*.log

# Claude Code (local settings, project skills, agent memory — not part of the repo)
.claude/

# Design mockups (reference only, not shipped)
mockups/

# Demo source/backup byproducts (only the optimized docs/demo.gif ships)
docs/demo.mp4
docs/demo-original-*.bak

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
